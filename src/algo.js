






var user = {{}} 


var currentUser 

user[]
    20answers[]
    20answersScore
    likedObjects[]
    20answersOrder
    

currentUser
    20answers[]
    futureObjects[]

var testSample = 





//recommendation algorithm (prototype -> check for errors and change variable names)










if(sampleSize>4){
    for(x = 0, x<20, x++){
        for(i = 0, i<testSample ,i++){
            if(user[i].20answers[x] == currentUser.20answers[x]){
                user[i].20answersScore=+1;
            }
        }
    }    

    for(x = 0, x<testSample, x++){
        for(i = 20, i>-1, i--){
            if(user[x].20answersScore == i){
                user[x].likedObjects(array2)currentUser.futureObjects;                  
            }
        }
    }
}

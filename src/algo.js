






var user = {{}} 


var currentUser 

user[]
    20answers[]
    20answersScore
    likedObjects[]
    20answersOrder
    

currentUser
    20answers[]
    futureObjects[]

var testSample = 

for(x = 0, x<20, x++){
    for(i = 0, i<testSample ,i++){
        if(user[i].20answers[x] == currentUser.20answers[x]){
            user[i].20answersScore=+1;
        }
    }
}    

for(){
    for(){
        if(user[x])
    }
}
